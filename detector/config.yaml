# detector/config.yaml - COMPLETE WORKING VERSION
slack:
  webhook_url: "PLACEHOLDER_URL"  # REPLACE WITH ACTUAL WEBHOOK URL

detection:
  z_score_threshold: 3.0
  rate_multiplier_threshold: 5.0
  sliding_window_seconds: 60
  baseline_window_minutes: 30
  baseline_recalc_interval_seconds: 60
  error_multiplier_threshold: 3.0
  min_data_points_for_baseline: 10  # ADD THIS LINE

blocking:
  backoff_schedule:
    - "10m"
    - "30m"
    - "2h"
  permanent_after_attempts: 3

audit:
  log_file: "/var/log/detector/audit.log"

dashboard:
  port: 8080
  refresh_interval_seconds: 3
  host: "0.0.0.0"

monitoring:
  log_file_path: "/var/log/nginx/hng-access.log"